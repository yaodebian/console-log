# Engineering Series

## 有的时候我们会把图片转换成 base64 ，为什么？

图片转 Base64 的意义是减少 HTTP 请求。
在 HTTP/1.1 下，浏览器对同域名的并发连接数有限，一般只有 6 个。
如果页面上很多小图片都发请求，就会占满这 6 条连接，使关键资源比如 JS、接口请求排队等待，造成阻塞。
Base64 能减少这种无意义的小请求，从而避免连接池被占满。
但在 HTTP/2 之后，多路复用解决了这个问题，Base64 的价值就下降了。

由于TCP的队头阻塞，http2.0仍然可能存在阻塞，但相比于 http 1.1 ，应用层阻塞要小得多。